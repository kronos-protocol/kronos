#ifndef FRAME_METADATA_H
#define FRAME_METADATA_H
#include <stdint.h>

typedef enum {
    FLAG_COUNT
} MetadataFlagPosition;

static const uint8_t KRS_METADATA_FLAG_POSITION_SIZE[] ={

};

#endif //FRAME_METADATA_H
